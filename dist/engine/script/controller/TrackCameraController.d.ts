import { Vector2 } from "three";
import { Component } from "../../hierarchy_object/Component";
import { ComponentConstructor } from "../../hierarchy_object/ComponentConstructor";
import { GameObject } from "../../hierarchy_object/GameObject";
export declare class TrackCameraController extends Component {
    protected readonly _disallowMultipleComponent: boolean;
    protected readonly _requiredComponents: ComponentConstructor[];
    private _trackTarget;
    private readonly _targetOffset;
    private _cameraDistanceOffset;
    private _pixelPerfectUnit;
    private _pixelPerfect;
    private _lerpTrack;
    private _lerpAlpha;
    protected start(): void;
    private readonly _tempVector;
    update(): void;
    setTrackTarget(target: GameObject): void;
    get targetOffset(): Vector2;
    set targetOffset(value: Vector2);
    get cameraDistanceOffset(): number;
    set cameraDistanceOffset(value: number);
    get pixelPerfectUnit(): number;
    set pixelPerfectUnit(value: number);
    get pixelPerfect(): boolean;
    set pixelPerfect(value: boolean);
    get lerpTrack(): boolean;
    set lerpTrack(value: boolean);
    get lerpAlpha(): number;
    set lerpAlpha(value: number);
}
